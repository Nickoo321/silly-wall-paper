# Queued Opus brief: screen mirroring / kaleidoscope for every look (launch after the transparent-oil executor commits; before the lava-lamp brief)

Repo: "C:\Users\abg77\OneDrive\Desktop\wall paper engine\claude code". Read AGENTS.md, PROGRESS.md, WORKLOG.md tail first. Same hard rules as every executor: never touch build\ or the running FluidWallpaper.exe or Wallpaper Engine; build ONLY build2 from PowerShell via cmd /c "`"...\build2.cmd`" FluidWallpaper" and confirm the exe timestamp; every render holds build2/shots/gpu.lock (wait while it exists, delete in finally); headless --shot only; no %APPDATA% writes; fluid md5 10E36EBF1A74EDFE609065D757300054 must hold with the feature OFF (we-look-live.ini 60 s 2560x1440 seed 1234 --hdr on) — the transform must be a no-op path that does not change the fluid PSO's output when disabled (an `if (mirror.x > 0.5)` branch in the display shader is fine; NOT a new macro/PSO).

User (2026-09-17, showing two Steven McFarlane "Liquid Acid" loops that are 4-fold mirrored, left/right and top/bottom, with the seam through the centre): "mirroring the sim ... as a random wallpaper would be cool, shouldn't be hard". They also want it to fit the tray-preset system (one app, looks as modes, and possibly random selection later).

Implement in the DISPLAY pass (src/shaders.h, the shared display pixel shader; all three looks fluid/liquid_acid/ink get it because it is a uv transform applied FIRST, before the dye sample, before the acid metaball field (`pp` derives from uv) and before InkWater — so blobs, rims, swarms, speckle, everything mirrors consistently). New `[mirror]` ini section (+ FluidConfig fields, parse/write in src/main.cpp, a small "Mirror" group in src/settings.cpp with mode combo/slider):
- `mode` = 0 off (default) | 1 horizontal (left half mirrored to the right) | 2 vertical | 3 quad (both, the reference look) | 4 kaleidoscope (N wedges around the centre, `segments` default 6, each wedge alternately reflected so there is no seam discontinuity).
- `center` = "x y" (default 0.5 0.5): the fold point. For mode 1-3 the SHOWN source is the region on one side of the centre; make the shown half/quadrant selectable via `source` = 0..3 (which quadrant of the sim is shown, so the interesting part of the sim can be chosen) — implement as: fold uv toward the centre, then map the folded region onto the chosen source quadrant of the sim texture, stretched to fill it (so the full sim resolution is used: the quadrant fills the mirrored tile; nothing is wasted).
- `rotate_period` (s, default 0): slow rotation of the fold axes about the centre for kaleidoscope mode (and optionally quad); 0 = fixed.
- `drift` (0..1, default 0): slow wander of the centre point (fbm of time) so the seam is not glued to the screen centre.
- `soft` (uv units, default 0): a small crossfade band across each seam (sample both sides and lerp) so the fold line is not a hard mirror crease; 0 = hard mirror like the references.
Mouse / splat interaction: the sim still runs full-size; with mirror on, map the pointer position INTO the shown source region (inverse of the display mapping, choose the primary image) so clicking/holding still splats where the user sees it. If the pointer lands on a mirrored copy, map it back to the source. Keep it simple; document what is not mapped.
Cost must be negligible (a few ALU per pixel).

RENDER (960x540 unless noted, --hdr on, --shot-delay 60, --shot-yield 2, seed 1234): a sheet (reference/configs/ab.py, labels without '=') in build2/shots/mirror/ with: we-look-live off / quad; liquid-acid-a-real quad; layering1-09-real (magenta on black, build2/shots/explore/layering1-09-real.ini or reference/configs/) quad and kaleidoscope 6; ink-duo-pour-teal-vermillion quad with drift 0.5 at two times (t=30, t=90) to show the centre moving. Write reference/configs/mirror-quad.ini as an OVERLAY-style preset (just `[mirror] mode=3`) if the preset loader supports partial inis (it does: presets are overlays — check `--shot-preset` handling in src/main.cpp) and reference/presets/"Mirror - quad (overlay).ini", "Mirror - kaleidoscope 6 (overlay).ini", "Mirror - off (overlay).ini", so the user can toggle mirroring on top of ANY look from the tray. Verify that applying the overlay preset at runtime (`--shot-preset <ini> AT 30`) mirrors the running look without re-creating PSOs.

Then fluid md5 check (mirror off), WORKLOG.md entry, commit (Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>). Report under 250 words: keys, sheet path, whether overlay presets work at runtime, pointer mapping status, md5, commit.

NOTE (user, 2026-09-17): "The reference photos were not meant to be copied, just vague ideas." Do not aim for photo parity, no side-by-sides against the refs; A/B on our own inis only.

DESIGN FOR LATER (do not build now): the same fold machinery will later drive a SWEEP transition between looks — the fold line becomes a split line, each side paints the same dye with a different look, and the line travels across (see PROGRESS.md "End state"). So structure the code as: (1) one function that returns, for a pixel, the folded/source uv AND a signed distance to the nearest fold line (with the soft band derived from it); (2) the line definition (centre, angle, mode) in one small struct/cbuffer block. Keep those two separable so a future "side weight" can reuse (1) without touching the mirror modes.
