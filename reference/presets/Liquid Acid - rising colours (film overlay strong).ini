; VARIATION: film overlay strong; everything else as shipped. Hairs in the gate, dust, scratches, a coloured leak, the finer noise layer and the stock's grade.
; Liquid Acid — LAVA LAMP: the tile9 family rising from below.
; Base: build2/shots/explore/tile9-08.ini (near-full-frame coloured oil with
; black holes over MONO ink — the family the user "really likes"), plus the
; real-oil edge set, the transparent film at the user's 0.5 + held chroma, and
; the rise block. The oil climbs at 0.015 uv/s with a lazy wobble and re-enters
; under the bottom edge; the metaball field merges and necks the lobes apart as they pass. Colour changes by stepping through the eight curated tile9 palettes (900 s per full trip).
; Bubbles stay black: the ink is mono and only the oil takes the hue.
[moods]
enabled = 0
migrated = 1

[cycle]
enabled = 0

[hdr]
peak_nits = 1000
compensation = 1
saturation = 1.20
brightness = 1.08
contrast = 1.00
knee = 0.70
gamut = 2

[sim]
sim_res = 256
dye_res = 4096
density_diffusion = 0.999
velocity_diffusion = 0.999
pressure_diffusion = 0.85
pressure_iterations = 20
vorticity = 12
splat_radius = 0.64
decay_fast = 1.000
decay_threshold = 0.290
saturation_restore = 0
max_brightness = 1.35
shading = 1
dye_diffusion = 0.000
baroclinic = 0.000
flow_speed = 1.00
gravity = 0
gravity_pow = 1.5
gravity_blur = 3

[behavior]
color_cycle_period = 19
wanderers = 0
wanderer_count = 2
wanderer_mode = 0
wanderer_speed = 246
wanderer_brightness = 0.10
wanderer_scale = 0.10
wanderer_resume_delay = 4.5
auto_pause = 1
dark_floor = 9
dark_level = 0.070
surv_dark_floor = 8
contrast_req = 30
dart_enabled = 0
dart_interval = 7
dart_speed = 967
hueshift_enabled = 0
hueshift_step = 83
hueshift_linger = 6.5
hueshift_glide = 7.2
hueshift_burst_steps = 2
hueshift_off_time = 10
idle_splats = 0
idle_interval = 9.6
idle_amount = 8
idle_brightness = 1.50
hold_to_splat = 0
splat_on_click = 0
show_mouse = 0

[color]
colorful = 1
more_colors = 1
post_saturation = 1.14
post_brightness = 1.06
post_contrast = 1.34
post_hue = 14.4
hue_center = 0
hue_range = 180
hue_linger = 0
curve_enabled = 0
shadow_floor = 0.100
shadow_knee = 0.15

[general]
fps_limit = 240
mirror_second = 0
pause_on_fullscreen = 1

[look]
style = liquid_acid

[liquid_acid]
rim_vary = 0.4
rim_ink_follow = 0.5
blob_count = 96
disc_frac = 0.22
web_frac = 0.34
bubble_frac = 0.20
disc_min = 0.160
disc_max = 0.380
web_min = 0.070
web_max = 0.200
bubble_min = 0.015
bubble_max = 0.075
hole_min = 0.030
hole_max = 0.130
size_bias = 2.0
big_bias = 0.62
hole_weight = 0.608
; Coverage: the tile9 bases sit at threshold ~0.47-0.56 with repulsion
; 0.90 and NO rise. Under a constant rise the blobs bunch as they climb
; and that pair floods the frame into one continent (rendered: the oil
; filled ~90%). 0.85 + the tile9 repulsion puts the coverage back where
; the tile9 tiles sit while leaving the lobes separate enough to neck
; apart and merge on the way up, which is the lava-lamp read.
threshold = 0.85
support_scale = 2.00
flow_gain = 1.15
curl_drift = 0.0016
repulsion = 0.90
rim_width = 0.0016
rim_inset = 0.0022
rim_dark = 0.80
meniscus = 0.85
meniscus_width = 0.0020
meniscus_offset = 0.0030
meniscus_color = 0.02 0.02 0.02
swarm_clump = 0.85
swarm_dark = 0.35
refraction = 0.050
translucency = 0.10
ink_shading = 0.00
oil_texture = 0.0759
swarm_holes = 0
; --- DROPLET PARTICLE SIM -------------------------------------------------
; Real particles, not the screen-space cellular swarms: they are added into
; the SAME metaball field as the big blobs, so a trapped water droplet is a
; literal hole in the oil and an oil droplet on the ink a literal bump of it.
; They ride the oil they sit in, nucleate where the film is thin, attract,
; coalesce (area-conserving) and dissolve. swarm_holes/swarm_drops are set to
; 0 here explicitly -- the code forces them off anyway, but ONE system is the
; point and the ini should say so.
droplets = 950
droplet_spawn_rate = 40
; size/grouping step 1 (user 2026-09-18: fewer small, more mid, small ones grouped; ~10% per step)
droplet_r_min = 0.0011
droplet_r_max = 0.0132
droplet_attract = 0.50
swarm_drops = 0
swarm_density = 0.2674
swarm_scale_holes = 22
swarm_scale_drops = 30
swarm_r_min = 0.120
swarm_r_max = 0.420
swarm_rim_dark = 0.55
ink_levels = 4
ink_soft = 0.60
ink_mix = 0.95
ink_hue_vary = 3
ink_gain = 1.30
ink_bias = 0.03
seam_strength = 0.30
seam_lo = 0.06
seam_hi = 0.70
grain = 0.057
grain_scale = 3.0
speckle = 0.2227
oil_color_1 = 0.95 0.40 0.10
oil_color_2 = 0.98 0.45 0.08
oil_color_3 = 0.92 0.35 0.12
oil_color_4 = 1.00 0.50 0.05
ink_stop_1 = 0.086 0.478 0.494
ink_stop_2 = 0.016 0.040 0.046
ink_stop_3 = 0.478 0.165 0.020
ink_stop_4 = 0.980 0.545 0.078
ink_mode = water
ink_complement_lock = 0
; --- "real oil" edge set (54780a0): no stroked boundary, the film thins
; --- to nothing at its edge and the halo is emergent from the ink. ---
; Edge treatment. 1 = CRISP: a big hole ends on the same hard isoline the
; droplets do, with only its thin meniscus, instead of fading out over a wide
; translucent band that scales with its size. 0 = the soft film edge, shipped
; as "Liquid Acid - rising colours (soft film edge).ini". User 2026-09-17,
; shown stills of both: "hard to say in stills, apply all".
oil_edge_mode = 0
; --- hollow "lens" droplets, and the rafts they pack into (2026-09-17) ------
; The user, on two panel photos of the live rising look: "I like these empty
; doubles, you think you can add more of them", and, with photos of boiling oil
; and a soap raft: "it kinda looks like this, should probably clump". So a
; third of the trapped droplets are drawn as a thin dark RING with the oil
; showing through the middle, and rings attract each other and pack into rafts
; with a shared wall instead of merging. A solid droplet still absorbs a ring.
droplet_ring_frac = 0.15
droplet_ring_width = 0.13
droplet_ring_lift = 0.08
droplet_ring_clump = 0.6
droplet_ring_wobble = 1
; --- edge profile ----------------------------------------------------------
; "smudge the border more -- it looks like it goes from green to black, then
; stops; it should be more S-curved". Same band, smootherstep profile, so the
; slide from oil colour to black has no knee at either end.
oil_edge_curve = 1
; --- backlight penumbra ----------------------------------------------------
; The lamp is under the middle of the dish, so the oil right beside a black
; mass is lit less than the open sheet -- and a pigment lit less shifts hue as
; well as level. Very subtle on purpose: depth, not a coloured outline.
oil_penumbra = 0.3
oil_penumbra_px = 8
oil_penumbra_hue = 12
oil_penumbra_dark = 0.15

; --- macro "cellulose" surface texture (2026-09-18, brief O) ---------------
; The user, on the Requiem microscope frame: the cell body is fibrous and
; mottled, and they want that "in the black oil more than the oil". An
; anisotropic fBm stretched along a slowly turning direction (strands, not
; blobs), drifting with the rise, added in the display pass under the grain.
; On the black it is a THICKNESS effect: strongest just inside the edge of a
; black mass, gone deep inside it, so true black stays true black.
cellulose = 0.45
cellulose_ink = 1
cellulose_oil = 0.35
cellulose_scale = 40
cellulose_drift = 1
oil_thin_edge = 0.70
oil_edge_frac = 0.14
oil_specular = 0.35
oil_iridescence = 0.20
swarm_lens = 0.80
meniscus_from_ink = 1.0
; meniscus_film_mix (2026-09-22, the auditor's ring-stack proposal) -- where
; the bright halo gets its COLOUR. The halo is the biggest thing painted on a
; droplet boundary: it replaces up to `meniscus` (0.85 here) of the pixel,
; and with meniscus_from_ink 1 it is derived from the INK, so it sits on top
; of and hides every film-hue feature -- hue2, the seam reflect, the sweep.
; The two terms that do carry the film's colour outward (mass_rim, the
; bright-field halo) peak at 0.098 and 0.072 against it. Raised, the halo
; takes its colour from the FILM at that droplet (the same oilR its lit rim
; uses, so a hue2 SEAM reaches the halos around it), lifted by the same
; factors the ink path uses so it stays a bright caustic, and it opens by the
; same amount the ink-brightness gate meniscus_from_ink puts on the halo.
; That gate has to open or the key does nothing here: MEASURED 2026-09-22,
; with the ink outside the oil black in every pixel the gate is 0 everywhere,
; so `meniscus = 0` renders byte-identically to `meniscus = 0.85` and so does
; `rim_dark = 0`. The two terms at the top of the ring stack paint nothing in
; this preset today. The band keeps its shape, width and position and the
; dark hairline keeps the original gate. 0 = as shipped, byte-identical;
; left at 0 until the A/B is judged (A 0 / B 0.5 / C 1.0).
meniscus_film_mix = 0
oil_glow = 0.45
refraction_width = 22

; --- transparent coloured film (08a89aa) at the user's pick: 0.5 with the
; --- chroma held back to the opaque level (measured x1.19 on tile9-08). ---
oil_transparency = 0.5
oil_absorb = 2.6
oil_film_bump = 0.35
oil_refract_body = 0.006
oil_ink_blur = 0.5
; USER PICK (2026-09-17): oil_transparency 0.5 with the chroma HELD to the
; opaque level -- "the 50% chroma held is the best". post_chroma scales the
; finished frame's colourfulness about its own luma; measured on tile9-08 at
; t=75, OKLab means over the non-dark pixels: opaque L .663 C .194, film 0.5
; alone L .575 C .169, film 0.5 + post_chroma 1.2 / post_lift 1.08 L .63 C .20.
post_chroma = 1.2
post_lift = 1.08

; --- LAVA LAMP rise ---
; one screen height in ~65 s
rise_speed = 0.0195
rise_wobble = 0.5
rise_respawn = 1
rise_stretch = 0.45
rise_bottom_light = 0.5
oil_saturation = 1.0
hue_sweep_period = 630
hue_rotate_period = 0
; --- the eight tile9 palettes, VERBATIM (twelve floats = the four
; --- oil_color_N shades of that tile, not re-derived from an anchor).
; --- The ink anchors are mono: the water under the oil stays grey and
; --- the bubbles/holes stay BLACK through every hue.
sweep_count = 8
; hot pink
sweep_oil_1 = 0.98 0.20 0.50 0.95 0.18 0.45 1.00 0.25 0.55 0.92 0.15 0.42
sweep_ink_1 = 0.120 0.120 0.120
; magenta
sweep_oil_2 = 0.88 0.12 0.62 0.85 0.10 0.55 0.90 0.18 0.68 0.80 0.08 0.50
sweep_ink_2 = 0.120 0.120 0.120
; violet
sweep_oil_3 = 0.55 0.15 0.90 0.50 0.10 0.85 0.60 0.20 0.95 0.48 0.08 0.80
sweep_ink_3 = 0.120 0.120 0.120
; cyan
sweep_oil_4 = 0.10 0.80 0.95 0.08 0.75 0.90 0.12 0.85 1.00 0.05 0.70 0.88
sweep_ink_4 = 0.120 0.120 0.120
; teal
sweep_oil_5 = 0.05 0.75 0.70 0.08 0.80 0.75 0.02 0.70 0.65 0.10 0.85 0.78
sweep_ink_5 = 0.120 0.120 0.120
; lime
sweep_oil_6 = 0.60 0.90 0.10 0.65 0.95 0.08 0.55 0.85 0.12 0.70 1.00 0.05
sweep_ink_6 = 0.120 0.120 0.120
; orange
sweep_oil_7 = 0.95 0.40 0.10 0.98 0.45 0.08 0.92 0.35 0.12 1.00 0.50 0.05
sweep_ink_7 = 0.120 0.120 0.120
; red
sweep_oil_8 = 0.85 0.10 0.08 0.90 0.12 0.06 0.80 0.08 0.10 0.95 0.15 0.05
sweep_ink_8 = 0.120 0.120 0.120

oil_drag = 0.9
oil_dye_block = 0.7
rise_parallax = 0.7
rise_parallax_dim = 0
oil_viscosity = 0.6
mouse_oil_mode = 0
; --- per-droplet DEPTH (2026-09-18, brief N) -------------------------------
; Every droplet and ring sits at its own distance from the lens, spread about
; the masses' own plane; the back layer climbs a little faster than the front
; (the user's idea), so the depth reads in the motion as well as in the blur.
; Does nothing without [post] dof_max_px.
droplet_depth = 0.35
depth_rise = 0.22
; --- DIFFRACTION, the size side (2026-09-18, brief W) -----------------------
; The other half of the point spread: a feature narrower than it has light
; filled in from all round it and cannot reach full darkness. Tiny droplets
; come out as soft grey dots, medium ones dark with soft edges, only the big
; masses go truly black. It only ever takes darkness away from small things,
; so the black under a mass is untouched.
diffraction = 1
diffraction_px = 1.5
; --- DROPLET LENS SHADING (2026-09-18, brief X) ----------------------------
; The user, on the oil-and-water reference against our live frame: "I was
; thinking of the oil boundary layer -- some mechanics of how it should be
; gradual -- and it translated to it just being blurry. The ratio of blurry to
; focused is off." In the reference every droplet down to four pixels is CRISP
; at its edge and GRADUAL inside it: a thin bright refractive rim just outside,
; a darker band just inside, a lighter middle (the droplet is a lens focusing
; the backlight) and a small specular from the lamp. None of this adds blur --
; the crispness is the edge, the gradualness is the interior, and the specular
; reads the same rig the haze and the bloom do, so it swings with the lamp.
droplet_lens = 0.55
droplet_lens_centre = 0.30
droplet_lens_band = 3.0
droplet_spec = 0.35

; --- CONSERVATION OF MASS (brief S, 2026-09-18) ---------------------------
; Nothing appears or vanishes at a visible size in the open: droplets that
; leave the frame re-enter from the opposite edge with their size, ring and
; seed intact, a blob only wraps once its own field is clear of the frame, a
; new droplet swells from below the resolvable floor over spawn_grow_s, and
; one dying of old age fades out over dissolve_s.
conserve_mass = 1
spawn_grow_s = 4
dissolve_s = 5
; --- COALESCENCE (user 2026-09-18, panel photo lobed-groups-of-5) ---------
; "all of these things are groups of 5, like what". droplet_merge's overlap
; distance sits inside the contact repulsion's rest distance, so it was
; unreachable and touching droplets stayed necked together as a permanent
; lumpy 4-6 lobed cluster. Now a contact coalesces at 8/s, area-conserving,
; and the neck closes over a second into one round droplet. Rings keep their
; foam rafts with shared walls.
droplet_coalesce = 1
droplet_coalesce_s = 1.0

; --- BIG HOLLOW BUBBLES (user 2026-09-18, photo big-hollow-bubble-liked) --
; "the big faint transparent lens in the middle-left is exactly what I want
; more of". Rings used to share the solid droplets' radius range, which is
; itself clamped by the shader's 3x3 cell walk, so a hollow bubble could never
; be more than a small ring. A big ring is now registered in every grid cell
; its support covers, which restores the walk's invariant, so it may be up to
; droplet_ring_r_mul times droplet_r_max. It is born at nothing and swells
; (conserve_mass), keeps the same thin wall as a fraction of its radius, and
; only a small share of new rings are big -- a couple visible at a time.
droplet_ring_r_mul = 3.5
droplet_ring_big_frac = 0.10

; --- RACING MICRO-BUBBLES (brief Y, user 2026-09-18 19:50) ----------------
; "I just saw racing small bubbles -- add more of those, like in water the
; uber small bubbles that go up: still slowish but considerably faster than
; the others." A share of the SMALLEST trapped droplets climb a multiple of
; their neighbours' speed with a lazy lateral spiral, born off-frame below the
; bottom edge and wrapping (conserve_mass), so one is never seen to appear
; mid-climb. racer_r_max is the smallest size that still RESOLVES on the panel
; (1.6 * rim_width is the contribution gate's floor, ~0.0026), not 2 * r_min,
; which would be invisible.
droplet_racer_frac = 0.25
droplet_racer_speed = 2.0
droplet_racer_wobble = 0.5
droplet_racer_r_max = 0.0050

; --- BUBBLE WEATHER (U9, 2026-09-18) --------------------------------------
; The droplet population has one steady state and settles into it. Weather
; gives it seasons on a minutes-long clock -- a spell of rings and big hollow
; bubbles, then of solids, a sparse spell, a crowded one -- by walking the
; target count and the ring shares between hashed per-phase targets. Never a
; jump and never a mass kill: the target moves smoothly and conserve_mass does
; the rest (surplus dissolves, shortfall grows in).
weather = 0.5
weather_period_s = 300
; --- THE DYE'S OWN DEPTH (2026-09-18, brief AA) ----------------------------
; The user: "the bottom right blob isn't getting more out of focus even as it
; approaches the edge." True, and by construction -- the per-pixel depth
; accumulator was primed with a constant 0.5, which is exactly camera_focus,
; so every pixel with no droplet in it (i.e. every big dye mass) sat ON the
; plane of focus by definition and could only blur through the shape of the
; focus surface. The dye is a layer at its own height now, with a gentle slope
; along the direction of the rig's lamp, so the slab and the focus surface are
; not parallel: they cross on a LINE instead of agreeing over a region, and
; that line travels as the lamp drifts. dof_max_px goes 5 -> 9 with it: the
; corners were already reaching the old clamp, so the extra depth had nowhere
; to show.
dye_depth = 0.5
dye_depth_tilt = 0.28
dye_depth_w = 0.25
droplet_mass_bias = 0.6
droplet_crust_density = 2.5
droplet_crust_r = 0.6
mass_rim = 0.35
; --- THE LAMP CASTS SHADOWS (brief BC) ------------------------------------
; The user: "the light should cast shadows essentially... it can be from
; behind, or the bottom, or the top or side." Until now the lamp lit the
; frame and nothing in it blocked any of that light. Now every mass and every
; droplet darkens the film on the side AWAY from the lamp -- which, with
; light_y 1.2, is UP -- tightest and darkest against the caster and softening
; as it runs out. A droplet's shadow is as short as the droplet is tall.
;   shadow_amt   0.40: a real shadow, not a heavy one ("I really don't see
;                the shadows that well", so well above invisible, well under
;                the point where the film above a mass goes muddy)
;   shadow_len   the longest reach, as a fraction of the screen height
;   shadow_soft  how fast the penumbra opens up along the shadow
; The lamp's own height is [post] light_z, down in the camera block.
shadow_amt = 0
shadow_len = 0.12
shadow_soft = 0.50

; --- MULTICOLOUR OIL (brief AE, ref oil-colour-combo-ref-1.jpg) -----------
; A second dye hue living IN the thin film: soft patches that the sim's own
; velocity advects, so they stretch and fold with the flow instead of sliding
; over it. The reference the user kept is a magenta film carrying cyan patches
; that read like a light leak -- but a leak is additive and would lift the
; black corners, and there the cyan sits UNDER the masses, which only a dye
; can do. The masses stay black over it; the droplets inside them take the
; same hue (crust_hue_mix), which is the ref's cyan-lit specks in the black.
; This palette sweeps through eight hues, so film_hue2 is a ROTATION off
; whatever the film currently is: -140 deg turns the hot pink toward cyan and
; keeps the same relationship at every step of the sweep. The amount is how
; far AROUND the wheel a patch goes, not a cross-fade to the far colour --
; blending magenta with cyan in RGB is grey -- so 1.0 is what lands the
; patch cores on the reference's cyan. Half of it would be blue.
film_hue2 = 0
film_hue2_amt = 0
film_hue2_scale = 0.35
film_hue2_drift = 1.0
film_hue2_decay = 0.35
film_hue2_wobble = 0
film_hue2_wobble_period = 300
film_hue2_seed_rows = 2
film_hue2_rise = 0.25
film_hue3 = 0
film_hue3_amt = 0
crust_hue_mix = 1.0
; --- SEAM REFLECTION REACH (2026-09-22, brief AJ) --------------------------
; The user, watching the hue2 seam live: "whatever algo is mixing the oil
; boundary is insanely good", "and the way the bubbles reflect it,
; accurately", "chef's kiss" -- then "turn up the radius of effect maybe, so
; further particles also reflect it on the boundary", and on the marked-up
; photo, two red lines: the seam's colour should reach the droplets up to
; about a THIRD of the screen height above it, fading with distance.
; Before this key a droplet's lit rim, lens highlight and glow were all
; tinted with the film colour at the droplet's own pixel, so a droplet only
; carried the seam's magenta while it stood inside the field's 0.56..0.68
; transition band -- about one droplet across at patch size 0.35. That band
; width was the whole reach, and 0 keeps it.
; 0.33 = the user's far line. The rims (never the film) rotate toward the
; seam's own hue -- the band's midpoint, which off this blue is the magenta
; in the photo -- with a smooth falloff, so the near droplets keep exactly
; the colour that earned the "chef's kiss" and the far ones pick it up
; faintly. Nothing is blurred: the rims stay as sharp as they were.
boundary_reflect_r = 0
boundary_reflect_amt = 1.0

; --- DYE THE BLACK (2026-09-22, brief AG / AM) ------------------------------
; The user: "add ability to dye the black ink", and note 11, "make the
; currently black oil mostly purple". The references are lava lamps -- amber
; wax over orange -- where the dark body is not black at all but a DEEP
; translucent colour with the lamp showing through.
; READING NOW (2026-09-22, branch dye4). The two earlier attempts dyed the ink
; RAMP and rendered byte-identical, because this preset runs ink_mode = water
; and in water mode the mass colour comes from InkWater()'s paper_color -- the
; ramp is never read (full trace at "DYE THE DARK MASSES" in src\shaders.h).
; The dye is now applied in the display pass on the ink layer itself, as a
; translucent wax: dye_lum is the light the THIN edge passes and the thick core
; keeps about 40% of it, so a big mass is darkest where it is deepest and glows
; at its rim, exactly like the lavalamp-ref-4/5 wax. The film is untouched, and
; the crust droplets and mass_rim still read against the body.
; VALUES (sheet build2\shots\live\dye4-sheet.png, hue 250/285/320 x lum
; 0.16/0.28/0.44, seed 1234, t=60 s):
;   hue 250 goes blue-violet and reads cold next to the magenta film;
;   hue 320 lands on the film's own magenta and the mass turns muddy;
;   hue 285 is the purple-violet of note 11 and holds apart from the film.
;   lum 0.16 is still basically black, 0.44 lifts the mass until it stops
;   reading as the dark body. 0.30 is the deep wax with the core still dark.
; dye_hue_follow is left at 0 on purpose: with 1 the wax tracks the film hue
; and would turn orange when the 8-pair sweep reaches its green family, and
; note 11 asked for purple. The cost is that when the sweep reaches its own
; violet pair (sweep_oil_3) the wax and the film sit close together -- worth
; its own A/B before changing this.
dye_hue = 0
dye_sat = 0
dye_lum = 0
dye_hue_follow = 0

[ink]
inverted = 1
chroma = 0
paper_color = 0.00 0.00 0.00
tint_thin = 0.85 0.85 0.85
tint_thick = 1.00 1.00 1.00

[post]
post_glow_dark = 0.6
post_glow_px = 10
post_glow = 0.22
post_blur_px = 0
; The CAMERA, and it is a MICROSCOPE, not a film stock: oil under a macro lens.
; The user's own summary of the reference photos is "super super slightly
; doubling plus noise", so everything optical here errs subtle and every knob
; is left for them to push.
;   softness  nothing in a macro shot is razor-sharp -- "these edges are way
;             too accurate and focused". Defocuses the isolines themselves
;             (coverage band, film edge, rim, meniscus); the grain stays sharp.
;   halo      the bright-field double contour: a weak, WIDE glow hugging the
;             outside of every dark shape with a faint darker echo beyond it.
;   aberration a warm/cool fringe along each edge, under a pixel wide: present,
;             minor, and only visible if you look for it.
;   film_grain real film grain, luminance-weighted, clearly there.
;   vignette  a gentle field-stop fall-off, never a circle.
film_grain = 0.11
film_grain_size = 2.5
film_grain_speed = 1
film_grain_color = 0
film_grain_chroma = 1.0
film_grain_density = 0
softness = 0.5
halo = 0.12
halo_px = 12
aberration = 1
aberration_px = 1.8
aberration_field = 1.2
; brief BD: the split is a difference of two resamples of the SHARP frame,
; so at 0 it stays razor-sharp on a bokeh disc, which no lens does. 1 =
; it fades with each pixel own blur. 0 is the uniform split the user
; approved on 2026-09-19, so this is the one knob to flip back first.
aberration_coc = 0
vignette = 0.12
band_min = 1

; --- FILM OVERLAY (2026-09-18, brief P) ------------------------------------
; The artefacts of a projected film, procedural and image-space: hairs caught
; in the gate, dust, fine scratches, the odd coloured light leak. Additive and
; weighted into the DARK pixels ("the black oil more than the oil"), authored
; at 1440p and scaled with the frame, and the population CHANGES -- the dust
; flickers every film frame, a hair sticks for a few seconds, a scratch
; persists for a stretch and drifts away. "A very very subtle thing" (the
; user), so these are starting points: every one of them is a Settings slider.
film_dust = 0.30
film_hairs = 0.40
film_scratches = 0.30
film_leak = 0.25
film_artefact_rate = 5
; a second, finer and faster noise layer under the coarse grain
film_noise = 0.10
film_noise_size = 1
; the stock's own grade, last of all: lifted teal shadows, warm highlights
film_stock = 0.35

; --- LIGHT IN THE WATER (2026-09-18, brief Q) ------------------------------
; The reference is a glass of cloudy water with a lamp under it: the water
; itself glows, brightest near the lamp and fading with distance, and the
; bright film bleeds a very wide, very weak wash into the black. One off-view
; light position drives both, and it never sits still -- a sum of slow sines
; over seconds to a minute, the frame's own idle animation. The haze falls to
; EXACTLY zero away from the lamp, so the OLED's black is still black.
fog = 0.35
fog_px = 700
; brief BD: the haze is the water and a mass floats in front of it, so it
; must not glow through the middle of one. 0 = the old behaviour.
fog_mass_gate = 0
bloom = 0.30
bloom_px = 140
light_x = 0.5
light_y = 1.2
light_drift = 1
; light_z (brief BC) is how far the lamp stands off the PLANE of the dish.
; 0.35 = a little in front of it, which rakes the cast shadows away from the
; lamp at about the length the caster is tall. Sink it toward 0 for long
; grazing shadows; take it negative and the lamp is BEHIND the dish, so every
; caster spills its shadow evenly onto the film in front of it instead.
light_z = 0.35
; --- PERSPECTIVE CAMERA + DEPTH OF FIELD + TILT (2026-09-18, briefs N + R) --
; The camera stops being an orthographic scanner. It is a LENS at the tip of a
; view cone looking down at the dish, so:
;   camera_fov          off-axis rings are seen at an angle: foreshortened
;                       radially toward the axis, the far side of the wall
;                       thicker than the near side, the highlight swung toward
;                       the axis. 0 = the old symmetric stamped circles.
;   dof_max_px          each element is blurred by how far its OWN depth is
;                       from the plane of focus, up to this radius, instead of
;                       one blur for the whole frame. post_blur_px is the floor
;                       under it -- the lens's own softness.
;   camera_field_curve  the surface of focus bends away from the flat dish with
;                       distance from the axis, so the centre and the corners
;                       cannot both be sharp.
;   focus_tilt          ...and it is slanted, freelensing style: a band of
;                       sharpness focus_band_px wide across the frame.
;   focus_tilt_period   the tilt and the focus distance do NOT drift. They hold
;                       still for about this many seconds, then somebody
;                       re-tilts the lens over focus_tilt_move_s -- eased, with
;                       a slight overshoot and settle -- and hold again.
camera_fov = 28
camera_focus = 0.5
camera_field_curve = 0.12
camera_axis_x = 0.5
camera_axis_y = 0.5
focus_tilt = 0.26
focus_tilt_angle = 25
focus_band_px = 340
focus_tilt_period = 75
focus_tilt_move_s = 1.4
dof_max_px = 9
; --- DIFFRACTION (2026-09-18, brief W) -------------------------------------
; The user, on a photo of an in-focus ring: "a thin hair on a film will never
; cause pure darkness because the light bends around it; it's not out of focus
; per se." psf_px is the lens's POINT SPREAD and it is the floor under the
; defocus radius whatever the focus -- "razor sharp" means diffraction-limited,
; not pixel-limited, and at 1 px an in-focus isoline can never come out as the
; stair-stepped coverage AA the photo caught.
psf_px = 1.0
; --- OUTPUT DITHER (2026-09-18, brief V0) ----------------------------------
; The panel quantises to 10 bits, and on a big saturated flat -- which is most
; of this look -- the user can see the steps on the OLED. Half an LSB of
; ordered noise on the finished frame breaks them into a pattern the eye
; integrates away. It fades out into true black, so an off pixel stays off.
dither = 1
; --- HALATION (2026-09-18, brief V1) ---------------------------------------
; CineStill 800T is ordinary cinema stock with the anti-halation backing
; removed: light from a highlight crosses the emulsion, reflects off the base
; and comes back a few pixels out, reddened by the layers it has now crossed
; twice. Tight (not the wide weak `bloom`), taken only from what is genuinely
; bright, landing in the DARK around a highlight rather than on it -- the
; difference between the film glowing and the picture being over-exposed. It
; pools toward the rig's lamp, so its asymmetry turns as the lamp wanders.
halation = 0.25
halation_px = 14
halation_warmth = 0.75
; --- MOTION (2026-09-18, brief V3 / U 4,7,11,12) ---------------------------
; Nothing may sit at a fixed screen position on an OLED, ever, and the motion
; model is specific: a SLIGHT, SLOW drift all the time, plus occasional
; readjustments where EVERYTHING moves at once, eased, with a settle. Never
; constant visible motion, never one effect moving alone.
;   shimmer         the air above the lamp: a fine refractive wobble, strongest
;                   near the light, advected by the sim's own velocity field.
;   vignette_wander the vignette's centre follows the rig's lens, so the
;                   darkest corner turns over minutes.
;   pixel_shift_px  the safety net: the whole frame walks a many-minute orbit
;                   a few thousandths of a pixel per frame.
;   rig_readjust    how far the lamp and the lens centre re-aim when the focus
;                   readjusts, so the whole rig moves as one body.
shimmer = 0.35
shimmer_px = 0.8
vignette_wander = 0.6
pixel_shift_px = 3
rig_readjust = 0.5
lid = 0.45
lid_ghost = 0.24
lid_ghost_spread = 1.0
lid_rings = 0.25
lid_sheen = 0.04
lid_sheen_px = 400
lid_glint = 0.15
lid_iris = 0.22
lid_refract_px = 2.5
