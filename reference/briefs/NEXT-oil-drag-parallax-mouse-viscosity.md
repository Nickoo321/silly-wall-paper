# Queued Opus brief: oil as an obstacle to the ink flow + lava-lamp parallax (launch after the droplet executor commits)

Repo: "C:\Users\abg77\OneDrive\Desktop\wall paper engine\claude code". Read AGENTS.md, PROGRESS.md, WORKLOG.md tail, `git log -10 --stat` first (recent: 54780a0 oil film edges, 08a89aa transparent film, db43eb9 mirror, f5f3e30 lava rise + post_chroma, then the droplet-particle commit). HARD RULES: never touch the live wallpaper (runs from build2\live\FluidWallpaper.exe — do not kill/relaunch/signal any FluidWallpaper process, do not touch build\, build2\live\ or Wallpaper Engine; tools/away-pause.ps1 is running, leave it). Build ONLY build2 from PowerShell: cmd /c "`"C:\Users\abg77\OneDrive\Desktop\wall paper engine\claude code\build2.cmd`" FluidWallpaper" and confirm the exe timestamp. Every render holds build2/shots/gpu.lock (wait while it exists, create with name+time, delete in finally); headless --shot only; no windows; no %APPDATA% writes. Fluid look byte-identical: we-look-live.ini 60 s 2560x1440 seed 1234 --hdr on md5 10E36EBF1A74EDFE609065D757300054 (both features default 0 → the sim passes must be skipped entirely when off).

User (2026-09-17, watching acid-rise-12.ini live): (2) "make it impossible for the fluid sim underneath, the mono ink, to get under the oil, or rather an intense friction that makes it hard for it to get under." (3) "for the lava lamp effect make the smaller / further particles move slower, aka parallax."

IMPLEMENT
A. `[liquid_acid] oil_drag` (0..1, default 0) and `oil_dye_block` (0..1, default 0):
   1. Each frame, evaluate the oil COVERAGE at sim resolution into a small R8/R16 texture ("oil mask"): a tiny compute pass that sums the same Wyvill metaball field over AcidBlobs (and the droplet particles' grid if cheap; otherwise blobs only) and thresholds it with a soft edge (a few sim texels wide). Sim res is ~256x144, so this is negligible.
   2. In the velocity step (after projection or as a body-force/damping term before advection — pick what keeps the solver stable), multiply velocity by (1 - oil_drag * mask) with fps-normalised strength (per-second decay, like the other decays: k = 1 - exp(-rate*dt)), so ink under the oil comes to rest and new ink cannot stream in; at the oil EDGE the mask gradient should deflect flow around the island (add -grad(mask) * oil_drag * something as a gentle outward push, so ink piles up along the rim instead of leaking in).
   3. `oil_dye_block`: in the dye advection, attenuate dye that lands inside the mask (dye *= 1 - oil_dye_block * mask * k) so ink trapped under oil fades out over ~1-2 s and the oil reads as sitting ON the water. Keep ink outside untouched. Note the droplet holes: the ink visible inside holes is the ink layer, and holes are negative field, so the mask must be the oil coverage AFTER holes (holes = no oil = no drag), which the field threshold gives naturally.
   4. Mouse/hand splats are irrelevant here (all emitters are off in the rise presets), but the WE look must be unaffected (mask pass skipped when both keys are 0).
B. `[liquid_acid] rise_parallax` (0..1, default 0): in StepAcidBlobs the rise speed, wobble and flow response of each blob scale with its size: s = lerp(1, clamp(r / rRef, 0.25, 1), rise_parallax) with rRef = the disc_max radius; small blobs drift slower (further away). Optional `rise_parallax_dim` (0..1, default 0): small blobs slightly darker/less saturated (multiply their upload colour toward the ink/background by (1 - s) * dim) as a depth cue — keep subtle. Droplet particles (if they have their own motion) follow their parent blob, so they inherit it.
C. Mouse interaction for the oil look, as a MODE (user: "maybe make the mouse comb-like? it's all experiments, maybe oil mode just doesn't let you interact"): `[liquid_acid] mouse_oil_mode` = 0 none (DEFAULT, and what the shipped rise presets use: the cursor does nothing to the oil look at all) | 1 push | 2 comb; `mouse_oil_radius` (uv, default 0.12), `mouse_oil_gain` (default 1). The mouse must NEVER touch the ink in this look (rise presets already have show_mouse/hold_to_splat/splat_on_click = 0) and never delete/shrink oil.
   - push: blobs within the radius get a velocity impulse = pointer velocity * gain * kernel(distance) plus a weak radial push, so the cursor drags/parts the oil; holes and droplet particles inside move with their blob.
   - comb: the pointer path is a marbling comb. While moving, blobs within a NARROW band along the path get stretched along the drag direction: give AcidBlob an anisotropy (reuse the rise_stretch per-blob stretch + a direction, uploaded in .b.w / a spare float) that ramps up with pointer speed and relaxes back over ~3 s; small blobs are also dragged along. Result: thin oil streaks/trails behind the cursor that slowly round back up. No blob removal.
   Implement in StepAcidBlobs from the same input struct the splat code reads. Test hook: `--shot-mouse X,Y,DX,DY,T` (like --shot-drop) injecting one synthetic pointer move at T; render base vs push vs comb at T+1 s and T+4 s (relaxation) into build2/shots/oildrag/. Write reference/presets "Liquid Acid - rising colours (mouse push).ini" and "(mouse comb).ini" as variants; the main rising presets stay at mode 0.
D. `[liquid_acid] oil_viscosity` (0..1, default 0; user: "and more viscosity?"): the oil should move like a THICK liquid. One key that (a) raises the blob velocity damping and lowers flow_gain/curl_drift response (blobs lag the water, accelerate slowly, coast slowly), (b) slows breathing and the rise wobble, (c) makes merging/necking slow: when two blobs approach, add a short-range viscous attraction that pulls them together over seconds and a stronger contact softness so the neck forms and thickens gradually rather than snapping, and (d) passes a per-frame smoothing to the droplet particles' velocities so they never jitter. Also, if oil_drag is on, viscosity can raise the velocity diffusion of the SIM under the oil (thick fluid), otherwise leave the sim alone. Set ~0.6 in the rise presets.
Plumbing as the previous commits (fluid.h/fluid.cpp/main.cpp/settings.cpp sliders). Report the ms delta at 1440p.

RENDER (960x540, seed 1234, --hdr on): acid-rise-12.ini needs ink to show the effect — for the A/B use a copy with a slow `[drops]` emitter on (drops=1, interval 8, density 4) so white ink flows around/under the oil: base vs oil_drag 0.9 + oil_dye_block 0.7 at t=40 and t=80 (sheet in build2/shots/oildrag/); then rise_parallax 0 vs 0.8 at t=60 and t=90 (two frames so the differential motion is visible; label sizes). Set oil_drag 0.9 / oil_dye_block 0.7 / rise_parallax 0.7 in acid-rise-12.ini, acid-rise-rotate.ini and the two "Liquid Acid - rising" presets (leave the drops emitter OFF in the shipped inis). Fluid md5 check, WORKLOG entry, commit (Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>). Report under 300 words.
