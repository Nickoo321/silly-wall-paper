# Queued Opus brief: real HDR range for the ink look (and a chroma hold), plus a 240 Hz check
(launch after the rings/grain/aberration executor commits; one Opus at a time)

Repo: "C:\Users\abg77\OneDrive\Desktop\wall paper engine\claude code". Same HARD RULES as every executor (live exe build2\live untouched; build2 only via build2.cmd from PowerShell; gpu.lock around builds and renders; headless --shot only; no %APPDATA%; fluid look byte-identical: we-look-live.ini 60 s 2560x1440 seed 1234 --hdr on md5 10E36EBF1A74EDFE609065D757300054 — every new key defaults to the current behaviour). Process: engineering + one verifying shot per item, commit (Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>), STOP with a < 300-word report + a rote list for Sonnet (sheets, md5, nits measurements via the --shot HDR analyzer path, docs). Standing line: flag delegable parts / user decisions.

User (2026-09-17 22:45, on the panel with reference/configs/ink-duo-pour-teal-vermillion.ini live, Windows HDR on, panel brightness full; HDR analyzer readout: max 126 nits on the frame, 28 nits at the cursor; photo reference/shots/panel/2026-09-17-ink-compressed.jpg): "the colours are compressed — that's how it actually looks. Shouldn't it try to be brighter, or at least have more steps of brightness?" Vermilion reads as pale peach, teal as grey-blue.

Cause (read src/shaders.h ~1870-1882 and the ink composite; keys `[hdr] peak_nits` (700 in the inis), `brightness`, `gamut`, `hdr_core` (fluid.h ~478: "drives the HDR highlight gain"), `paper_color`): the only use of HDR headroom is the parity-plus highlight expansion: gain = lerp(1, peakGain, t^2) with t = smoothstep(knee, capBright, m) on RAW DYE INTENSITY. Designed for the WE fluid look. The ink look rarely reaches capBright, so the whole frame stays at SDR-parity level (≤ ~126 nits of a 1000-nit panel) and its tonal range is squeezed.

USER PRIORITY (22:55): "brightness isn't key in this mode, just avoiding compression / flatness." So the goal is TONAL SEPARATION and chroma, not peak nits: more distinct steps between the ink's darks, mids and lights, and saturated colours; keep the overall level near where it is (a modest white point, e.g. ~250-300 nits, not a bright frame). Contrast/separation first, brightness only as a side effect.

IMPLEMENT (ink style only; fluid path untouched; acid look: see item 3):
1. `[ink] tonemap` (0 = current, default) | 1 = HDR range mapping for the ink look: map the ink composite's linear output through a curve that uses the headroom across the WHOLE range, not only near the cap: `ink_white_nits` (the nits the brightest paper/ink colour should reach; default = the current effective value so 0-change; the presets get ~400) and `ink_black_nits` (floor, default 0), with a filmic/soft-knee curve (`ink_tone_knee` 0..1) so mids get more separation ("more steps of brightness") and highlights roll off toward `peak_nits` instead of clipping. Scale all channels together in linear light (hue/saturation preserved), like the existing gain does.
2. `[ink] chroma` (default 1 = unchanged): a chroma hold/boost in OKLab for the ink composite, same math as `post_chroma` in the acid look, so that tone mapping never desaturates (vermilion must stay vermilion when it gets brighter). Also check WHY vermilion currently reads as peach: if the ink composite blends dye with a bright paper/background colour in a way that washes chroma, fix that at the source rather than boosting after.
3. Make sure `peak_nits` from the ini / tray (CMD_PEAK_*) is what these curves target, so the tray peak menu works for ink too. Report what the acid look's composite does with the headroom (post_lift/post_chroma exist; does its brightest oil reach peak?) — do NOT change the acid look in this brief, just report numbers so the user can decide.
4. Verify with the --shot analyzer path or a nits readout from the PNG (scRGB 1.0 = 80 nits; document the conversion): report min / mean / max nits and a 10-step histogram for ink-duo-pour-teal-vermillion.ini at t=60 before and after, and confirm the fluid md5.
5. 240 Hz: the user's X27U is a 240 Hz panel and the analyzer showed 121 fps on the ink look. Check the render loop's pacing (vsync / present interval / any 144 cap, and the ink sim's per-frame cost at 1440p; does the ink path run a fixed number of sim substeps that scale with refresh?). Report the cause and the cost; fix only if it is a cap in our code (a present-interval or sleep), not a perf pass.
Presets: ink-* configs in reference/configs and the six "Ink - *" presets get tonemap 1 with values from your verification; leave the WE fluid presets untouched.
