; acid-rise-12-tone-half.ini -- the FINAL cycle's moderate acid-rise-12 stage (brief
; reference/briefs/FINAL-CYCLE.md A): acid-rise-12 exactly as it ships, plus the split
; tone at HALF strength. The user's split-tone A/B (monotone-post-0924, 2026-09-24) was
; "sometimes one, sometimes the other": the home stage monotone-post-0924 carries the
; full tone (shadow_tone 1), this moderate stage the other one (0.5). The tone keys
; below the amount (lift 0.06, sat 0.80, the complement hue, the 2225 s on/off clock,
; 120 s crossfades) are acid-rise-12's own, identical to the monotone's.
; A PARTIAL on [meta] base (applied first; relative to this file's folder): the tray,
; the settings window and the cycle director all resolve it.
[meta]
look = liquid_acid
base = acid-rise-12.ini

[liquid_acid]
shadow_tone = 0.5
