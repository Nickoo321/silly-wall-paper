# Audit 2026-09-22 — main @ 1ea3611 (after the AJ / refl merge; main since 48f6e95)

Auditor pass. No feature code touched. Scope: packed acid cbuffer, shader literal budget,
the droplet-boundary ("ring") stack, and EXECUTOR-CARD.md.

Overlap with briefs BE/BF/BG, filed while this ran: BE (named cbuffer fields) and BF (preset
identity script) are the right fixes for sections 1 and 4.5 and this audit does not re-argue them
— it supplies the numbers they should be checked against (the six stale/dead slots, 7 free slots
left). New here and not covered anywhere: the **peak-amplitude ordering** of the ring stack in
section 3, which changes what AF/AW/AJ should even be asked to do, and finding 4.1, a live
concurrency bug in the lock itself.

## 1. Packed acid cbuffer — slot table

Built from `UploadAcidConstants` in `src\fluid.cpp` against every `laP*.xyzw` read in the acid
literals in `src\shaders.h` (128 slots, p0-p31).

**No slot is written-but-misread, read-but-unwritten, or used under two meanings.** The packing is
actually sound. Every defect found is in the *documentation* of it — the comment table at
`src\shaders.h:565-607`, which is the only thing standing between an executor and a wrong slot.

| slot | comment table says | reality | verdict |
|---|---|---|---|
| `laP31.y` | `-` | `boundary_reflect_r`, read 2x | **STALE — introduced by my own AJ commit** |
| `laP31.z` | `-` | `boundary_reflect_amt`, read 1x | **STALE — same commit** |
| `laP19.w` | `-` | `(float)kAcidMaxDrops`, read 1x | STALE |
| `laP11.w` | `sweepDeg` | hard-written `0.0f`, **read 0x** | documented but DEAD |
| `laP29.y` | `rimW(uv)` | hard-written `3.0f / 1440.0f` | implies a key that does not exist |
| `laP26.y` | `1/cocSpan` | hard-written `1.0f / 0.12f` | magic constant, no key |

One-line fix for the two I introduced (`src\shaders.h:606`):
`float4 laP31;  // x crustHueMix  y boundaryReflectR  z boundaryReflectAmt  w -`

Genuinely free slots left: `laP11.w`, `laP16.z/.w`, `laP17.w`, `laP29.z/.w`, `laP31.w` = **7**.

## 2. Shader literal budget (MSVC cap 16380 bytes)

24 literals, none over cap. Three largest:

| bytes | headroom | lines | literal |
|---|---|---|---|
| 14081 | **2299** | 1089-1316 | `kDisplaySrc` |
| 13609 | 2771 | 3185-3414 | `kPostSrc` |
| 13582 | 2798 | 9-302 | `kComputeSrc` |

2299 bytes is roughly 30 lines of this project's commented HLSL. The chunk that overflows first is
the one nobody is currently editing, so the failure will arrive as a surprise build break in
whatever branch touches it next. Splitting the top three pre-emptively is a 10-minute rote job.

## 3. The ring competition

Every term that paints the droplet boundary, with the live amplitude in `acid-rise-12.ini` and the
fixed inner constant the shader multiplies it by. "Peak" = the two multiplied.

| term | key | ini | shader factor | peak | acts on | width source |
|---|---|---|---|---|---|---|
| meniscus halo | `meniscus` | 0.85 | `lerp(col, menC, w)` | **0.85** | replaces the pixel | `meniscus_width` 0.0020 |
| dark rim | `rim_dark` | 0.80 | `oilC *= 1-k*rimB` | 0.80 | darkens film | `rim_width` 0.0016 |
| swarm lens | `swarm_lens` | 0.80 | fringe 0.75 / ring 0.55 | 0.60 | trapped droplets | 0.26 x droplet r |
| thin edge | `oil_thin_edge` | 0.70 | `lerp(oilC, prod, tt^2*k)` | 0.70 | film -> ink product | `oil_edge_frac` 0.14 |
| droplet lens | `droplet_lens` | 0.55 | dome .30/band .30/rim .30 | 0.17 | every droplet | `droplet_lens_band` 3 px |
| oil glow | `oil_glow` | 0.45 | `+oilThinC * 0.45` | 0.20 | ink outside | `edgeW * 1.3` |
| penumbra | `oil_penumbra` | 0.30 | hue rotate + darken | 0.30 | oil side only | `oil_penumbra_px` 8 |
| mass rim | `mass_rim` | 0.35 | `+tint * 0.28` | **0.098** | mass edge, lamp side | **hardcoded 3 px** |
| bright-field halo | `halo` | 0.12 | `+bright * 0.60` | **0.072** | brighter side | `halo_px` 12 |

Eight of the nine paint the same few pixels. The ordering is the finding: the **meniscus** replaces
up to 85% of the boundary pixel and, with `meniscus_from_ink = 1.0`, its colour is derived from the
INK, so it is structurally immune to every film-hue feature. The two terms that actually carry the
film's colour outward — `mass_rim` and the bright-field halo — peak at 0.098 and 0.072.

**Proposal for AF/AW and AJ.** AF ("weak lid reflections"), AW ("weak lid effect") and AJ are three
requests for more of the same ~0.17 of budget, and each will keep reading as "weak" while an
ink-coloured 0.85 sits on top of it. They should not be judged, or tuned, one key at a time. Before
another amplitude key is added: one sheet of `acid-rise-12` at fixed seed/time with each of the nine
terms **soloed** (the other eight at 0), so the user can see what each one is really contributing —
that sheet is rote and cheap. Then the real question is a single key, not a tenth term: should the
meniscus take a share of the FILM's colour (`meniscus_film_mix`) instead of the ink's? If yes, AF,
AW and AJ are all mostly answered by it and should be re-judged on the panel afterwards, not before.
Note also that `mass_rim` — the term the user calls weak — has its width **hardcoded** at 3 px while
the comment table advertises `rimW(uv)`, so there is no slider to turn up even if it were wanted.

## 4. EXECUTOR-CARD.md

| # | issue | severity |
|---|---|---|
| 1 | `tools\gpu-lock.ps1` resolves the lock from **its own** repo root (`Split-Path -Parent $PSScriptRoot`). Dot-sourced from a worktree it locks `<worktree>\build2\shots\gpu.lock`, and `build2\` is gitignored — so **each worktree gets a private lock that excludes nothing**. With four branches in flight there are up to five independent "locks". The card does not say to use the MAIN repo's copy. Every concurrency guarantee the card claims is void unless executors happen to do what I did today. | **critical** |
| 2 | The lock rule was relaxed to "wait at most 2 min, then render anyway" and the rationale — `Never two --shot renders at once (2026-09-15: OLED went grey)` — was **deleted in the same edit**. The next executor cannot weigh a risk it can no longer see. The policy may well be what the user wants; the incident line should sit next to it, with what to do if the panel misbehaves. | **high** |
| 3 | Card mandates sending every sheet "via SendUserFile". No such tool is in the executor toolset. The rule is unactionable as written and silently skipped. | high |
| 4 | §3 says "Build with `cmd /c build-wt.cmd` in the worktree", but `build-wt.cmd` is in `.git\info\exclude`, so a fresh worktree does not have it. Every executor re-invents it (I did today). Either commit it or paste the cmake line into the card. | medium |
| 5 | Parity is defined only for `style=fluid`. There is no recipe for proving the ACID presets are byte-identical, which is the rule that actually bites on every acid key. I had to build a pristine-main worktree by hand. Recipe belongs in §3. | medium |
| 6 | Commit trailer says `Co-Authored-By: Claude Fable 5.1`. Actual practice on main is the model that did the work (Sonnet 5, Fable 5.1, Opus 5). Card should say "the model doing the work". | low |
| 7 | §4 "Current state" pins `main @ fd81f49`; main is `1ea3611`. §5 still lists AJ as "FUTURE, do not start". Both stale. | low |

## 5. Verified this pass

- Fluid parity `10E36EBF1A74EDFE609065D757300054` on the final AJ build.
- `acid-rise-12` at `boundary_reflect_r 0` = `CA1B5B2977964C112CEAD7D414815643`, byte-identical to a
  pristine-main build of the same preset. This is the acid-side check item 4.5 asks to formalise.
- Merged tree compiles clean (`fw-base` detached at 1ea3611, 8/8, FluidWallpaper.exe linked).
- `laP31.y/.z` do not collide with anything on merged main.

---

# Audit 2026-09-22 (2) — main @ ac62b77, the `jxr` merge (4b291c5)

`WriteShotPair` now writes four files. No shader change, so parity/preset identity are untouched by
this branch *by construction* (the `sdr`/`hdr` byte arrays are computed by the same statements; the
new code only reads `r/g/b` and writes into the new `half`/`pq` buffers).

## 6. Correctness — verified, not assumed

| item | check | result |
|---|---|---|
| 709->2020 matrix | vs BT.2087 | **correct**; all three rows sum to 1.000000 |
| PQ curve | m1 .1593017578125, m2 78.84375, c1 .8359375, c2 18.8515625, c3 18.6875 | **correct** ST 2084 |
| cICP chunk | read back from the shipped `jxr-acid12-pq.png` | **correct**: right after IHDR, data `[9,16,0,1]`, CRC valid, no `sRGB`/`gAMA` left |
| PQ png | IHDR | 16-bit, colour type 2 — as claimed |
| 8-bit PNGs untouched | diffed the encode path | **yes**, bit-identical statements |
| negatives survive | ran `tools\jxr-check.ps1` on the shipped `.jxr` | **yes**: 64bpp, 3.76% negative px, min ch -0.0607, max 5.7266 scRGB |
| `FloatToHalf` | sign, subnormal, overflow, NaN paths | correct; rounding is half-**up**, not half-to-even (comment says "round-to-nearest") — <=1 ULP, immaterial |
| COM | every interface is a `ComPtr`; `VT_R4`/`VT_BOOL` variants don't allocate | **no leak**. Four WIC factories are created per shot instead of one — waste, not a bug |

**SDR re-derivation <= 1/255: the claim holds, with ~30x margin.** half carries ~2.4e-4 relative
error; through `clamp(r/sdrScale)` and the sRGB encode the worst-case 8-bit deviation is ~0.03/255
(largest near white, where sRGB' is smallest). **But** `sdrScale` is not recorded in either HDR file,
so re-derivation needs the `[shot]` log line. Worth writing `sdr_white` into a `.jxr` XMP field or a
sidecar, or the claim is untestable six months from now.

## 7. Failure modes — two worth fixing

1. **Partial files are left on disk.** `InitializeFromFilename(..., GENERIC_WRITE)` creates and
   truncates before anything can fail. Any later failure (WIC absent, `SetPixelFormat` refused,
   `WritePixels` failure) leaves a 0-byte or truncated `.jxr` / `-pq.png` with a plausible name next
   to the good ones. Delete the target on the `false` paths.
2. **The silent one.** `okP = WritePng48(...) && PngSetCicpPq(...)`. If the PNG writes but the cICP
   rewrite fails, `okP` is logged FAILED yet a **valid, unmarked** PQ png stays on disk — and an
   unmarked PQ png is read as sRGB, which is exactly the "looks like fog" failure the function
   exists to prevent. It is the one output that is wrong in a way the viewer cannot detect. Write to
   `.tmp` and rename, and delete on failure. `PngSetCicpPq` also truncates the original in place, so
   a failed `fwrite` destroys a good file.

`SetPixelFormat` negotiation is handled **well** — the format is compared against the request and a
silent 8-bit fallback is refused with a log line. That is the right call and should be kept.

## 8. Disk growth — measured, not estimated

Per 2560x1440 shot, from `build2\shots\live`: `.jxr` 12.4 MB + `-pq.png` **21.2 MB** + `.png` 5.1 MB
+ `-hdr.png` 4.7 MB = **43 MB**. A four-value A/B sheet is 172 MB, and the main repo lives inside
**OneDrive**, which syncs gitignored paths — git ignores `build2\`, OneDrive does not.

**Rule to adopt:** the `.jxr`/`-pq.png` pair should be **opt-in** (`--shot-hdr-files`, default off),
because BG itself says they are "opened only when the user asks". Note the `-pq.png` is 1.7x the
size of the `.jxr` while carrying strictly *less* (Rec.2020-clipped, negatives gone) — if only one
survives, keep the `.jxr`. Second: shots belong outside OneDrive (worktree `build2\` already is;
the main repo's is not), or `build2\` should be excluded in the OneDrive client.

## 9. Merge order — the question mostly dissolves, but there is one real collision

Measured against `main @ ac62b77`, not assumed:

| branch | commits not in main | verdict |
|---|---|---|
| `jxr` | 0 | merged (4b291c5) |
| `shadow` | **0** | already contained in main — nothing to merge |
| `noise` | **0** | points at 4b291c5; no work committed yet |
| `menis` | 1 | `4d30545` = **my own pre-amend audit commit**, orphaned when I amended it to `5d271d4`. No meniscus work. Merging it would resurrect the stale AUDIT file and conflict with the current one. Reset `menis` onto main before any work lands on it. (My fault: never `--amend` a commit on a shared branch.) |
| `dye4` | 1 | real work, and it **collides** |

**`dye4` takes `laP31.yzw` for the mass dye colour. `laP31.y/.z` are `boundary_reflect_r` /
`boundary_reflect_amt` on main since 1ea3611.** dye4 branched at `abf2bf7`, before AJ. `git
merge-tree` confirms conflicts in `src\shaders.h`, `src\fluid.cpp` and the brief. The comment-table
conflict is textual and will be seen; the dangerous one is `float p31[4]` in `UploadAcidConstants`,
where a "keep both sides" resolution silently feeds a colour channel into the reflection reach.
**dye4 must be re-pointed before merge**, not merged-then-fixed. Free scalars on main are
`laP11.w`, `laP16.z/.w`, `laP17.w`, `laP29.z/.w`, `laP31.w` — seven, but **no free `.yzw` triple**,
so the dye colour has to be assembled, e.g. `float3(laP16.z, laP16.w, laP17.w)` with
`laP29.z` keeping dyeAmt as dye4 already has it.

**Order:** `dye4` alone, after re-pointing and re-running parity + preset identity; nothing else has
anything to merge. Then BE (named cbuffer fields) runs against a tree with no branches in flight —
which is what BE asks for, and it will be the last chance to catch a slot collision by eye rather
than by `git`.
