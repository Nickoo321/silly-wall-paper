# Queued Opus brief: Liquid Acid "rise" motion + per-blob colour rotation (launch after the oil-edge executor commits)

Repo: "C:\Users\abg77\OneDrive\Desktop\wall paper engine\claude code". Read AGENTS.md, PROGRESS.md, WORKLOG.md tail first. Same hard rules as every executor: never touch build\ or the running FluidWallpaper.exe or Wallpaper Engine; build ONLY build2 from PowerShell via cmd /c "`"...\build2.cmd`" FluidWallpaper" and confirm the exe timestamp; every render holds build2/shots/gpu.lock (wait while it exists, delete in finally); headless --shot only; no %APPDATA% writes; fluid md5 10E36EBF1A74EDFE609065D757300054 must hold (we-look-live.ini 60 s 2560x1440 seed 1234 --hdr on).

User's ask (2026-09-17, about the tile9 family: near-full-frame coloured oil with black holes over mono ink, build2/shots/explore/tile9-*.ini; they "really like that aesthetic"): "it's 12 different colors. Can be a system for changing colors once in a while. Maybe they spawn off screen and go up slowly maybe?"

Implement in src/fluid.cpp StepAcidBlobs (~line 2160) / UploadAcidConstants (~2240) / LiquidAcidConfig (src/fluid.h ~120-160) / ini parse+write (src/main.cpp ~340, ~1320) / settings.cpp sliders, all keys default 0 = current behaviour:
1. `rise_speed` (uv/s, default 0): constant upward drift added to every blob's target velocity (y is DOWN in uv, so subtract). With `rise_wobble` (0..1) a slow per-blob sinusoidal x wobble (use s1/s2 phases). Oil blobs and holes both rise; holes may rise a bit slower (holes are water: give them 0.7x).
2. `rise_respawn` (0/1): when rise_speed > 0 and a blob leaves the TOP (y < -wrapMargin - r), respawn it BELOW the bottom edge (y = 1 + margin + r) at a random x with a fresh radius from its kind's range, instead of wrapping in place. Population constant. Spawn must be staggered so the screen never empties: on look start, distribute blobs over the full height as now.
3. Colour: the user clarified "the hue of the ENTIRE screen can shift" — a global shift, NOT per-blob. Do not implement per-blob colour. Two global options, both keyed, so the user can pick on the panel:
   a. `hue_rotate_period` (s, default 0): continuous slow rotation of the whole oil palette's hue in HSV (AcidHueShift-style, saturation/value preserved; do it CPU-side on effOil in UploadAcidConstants, angle = 360*t/period). With mono ink and meniscus_from_ink the ink stays grey and only the oil turns. Note the earlier finding that not every hue is equally vivid at one S/V (yellow goes olive); accept it for this option, it is what the user asked for.
   b. the existing hue_sweep_period with the list extended to up to 12 entries (`sweep_count`, `sweep_oil_N`/`sweep_ink_N`, N=1..12; defaults unchanged = current 5): steps through curated vivid hues "once in a while" with the existing smoothstep cross-fade (long settled stretch, short fade).
4. reference/configs/acid-rise-12.ini: tile9-family base (copy build2/shots/explore/tile9-08.ini or -06.ini, whichever renders cleanest with the oil-edge keys from the previous executor's liquid-acid-a-real.ini), rise_speed ~0.015 uv/s (one screen height in ~65 s), rise_wobble 0.5, rise_respawn 1, hue_sweep_period 900 with the 8 batch hues in order (hot pink 0.98 0.20 0.50, magenta 0.88 0.12 0.62, violet 0.55 0.15 0.90, cyan 0.10 0.80 0.95, teal 0.05 0.75 0.70, lime 0.60 0.90 0.10, orange 0.95 0.40 0.10, red 0.85 0.10 0.08), meniscus_from_ink 1, hueshift_enabled 0. Plus acid-rise-rotate.ini: same but hue_rotate_period 600 instead of the sweep.
5. Verify with a --shot sequence (t=20, 60, 100, 140 s at 960x540, seed 1234) showing blobs entering from the bottom and the colour change (both inis); sheet via reference/configs/ab.py into build2/shots/rise/. Also a preset copy reference/presets/"Liquid Acid - rising colours.ini".
Then fluid md5 check, WORKLOG.md entry, commit (Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>). Report under 250 words: keys, what the sequence shows, paths, md5, commit.

6. Two user notes (2026-09-17): (a) "The bubbles stay black" — the holes/bubbles show the mono ink beneath and must STAY black/dark through every hue shift; only the oil changes colour. Verify in the sequence. (b) The user LOVES the tile9 batch's vivid colours ("those colors haiku just spammed out are top notch"): use the eight tile9 palettes EXACTLY as rendered (all four oil_color_N shades per hue, from build2/shots/explore/tile9-NN.ini: 01 hot pink, 02 teal, 03 red, 04 lime, 05 magenta, 06 violet, 07 cyan, 08 orange) as the sweep list and keep their coverage (threshold ~0.47-0.56, 64-96 blobs). Do NOT tone them down. Still add an `oil_saturation` multiplier key (default 1, HSV, CPU-side on effOil) so vividness can be dialled on the panel either way.

7. The target is a LAVA LAMP (user, 2026-09-17: "Do you see the lava lamp aesthetic now?"). Two extras, both keyed, default 0:
   - `rise_stretch` (0..1): rising blobs elongate along their velocity — cheapest route is an anisotropic kernel: pass a per-blob stretch (in AcidBlobGPU .b.w, currently unused) and scale q.y by 1/(1+stretch) in the field loop before d2 (small, fast blobs stretch most; relax toward round as the blob slows). Keep the gradient consistent with the scaled q.
   - `rise_bottom_light` (0..1): a gentle vertical brightness gradient on the oil (hotter near the bottom edge, cooling as blobs climb), applied to oilC and to the HDR lift, like a lamp base underneath. Subtle at 0.5.
   Motion feel: slow (one screen height per 60-90 s), long straight rises with a lazy wobble, blobs merging and necking apart as they pass (the metaball field does this already; keep repulsion low in the rise inis so they DO merge).

NOTE (user, 2026-09-17): "The reference photos were not meant to be copied, just vague ideas." Do not aim for photo parity, do not render side-by-sides against the refs; the refs only supply the ideas. Judge on whether our own frames look real and good. A/B is base-vs-new on our inis only.

8. Small extra (user, 2026-09-17, after the transparent film): the film lowers perceptual chroma by 10-20% and lightness by ~5-13% (OKLab means over non-dark pixels: laa opaque L .537 C .130 vs film .85 L .496 C .107; water L .553/.176 vs .482/.147; tile9 L .663/.194 vs .575/.169). Add `[liquid_acid] post_chroma` (default 1) and `post_lift` (default 1) applied to the FINAL acid composite `col` (after toe_tint, before the HDR step): chroma scaled about the pixel's own luma (hue and luma preserved; clamp), lift = a luma multiplier. The glass inis should set post_chroma ~1.2-1.4 / post_lift ~1.08 so the film looks as vivid as opaque; the user will pick on the panel. Post-processed previews of what that looks like: build2/shots/oil-trans/sheet-*-satheld.png.
   USER PICK (2026-09-17): "the 50% chroma held is the best" — oil_transparency 0.5 with chroma held to the opaque level (measured factor ~1.19 on tile9-08, ~1.18 on liquid-acid-a at 0.5). So: the lava/rise inis AND the glass presets use oil_transparency 0.5 + post_chroma 1.2 (+ post_lift ~1.05); make that the shipped default for the tile9 family presets. Re-render one A/B (opaque vs 0.5+post_chroma in-shader) to confirm the shader key reproduces the post-processed preview.

9. Small app change (user, 2026-09-17: "turn off the sim on my monitor when I'm on my phone"): manual pause (tray CMD_PAUSE=1, src/main.cpp ~line 2178) currently freezes the LAST FRAME on the OLED. Make a manual pause present ONE black frame (clear + Present once, then stop presenting) so the panel goes dark while paused; resume re-renders normally. Also add explicit WM_COMMAND ids CMD_PAUSE_ON=8 / CMD_PAUSE_OFF=9 (non-toggling) next to CMD_PAUSE so tools/away-pause.ps1 can stop tracking state; keep CMD_PAUSE=1 as the toggle for the tray menu. Verify headless if possible (a --shot run is not affected); otherwise reason from the code and note it as unverified on the panel. Fluid md5 unaffected (display path unchanged while rendering).
